/* Extracted from index.html in Stage 3; behavior-preserving. */
(function(){
  "use strict";
  window.ElectroLabDiagramBuilderHandoffV1025=Object.freeze({
    version:"10.25",
    exactPreset:"half_wave_rectifier",
    note:"Diagram actions now map to an exact Builder preset instead of a generic fallback."
  });
})();
